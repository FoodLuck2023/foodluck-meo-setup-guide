# Add these directives inside the target server/location block.
# Example:
# location /meo-manual/ {
#     auth_basic "FoodLuck MEO Manual";
#     auth_basic_user_file /absolute/path/to/.htpasswd;
# }

auth_basic "FoodLuck MEO Manual";
auth_basic_user_file /absolute/path/to/.htpasswd;
