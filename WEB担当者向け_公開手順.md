# FoodLuck MEO操作説明書サイト 公開手順

## 概要

このフォルダーは、FoodLuck MEO操作説明書の静的Webサイト一式です。

サーバー側でWordPressやデータベースは不要です。HTML、CSS、JavaScript、画像、PDFだけで動きます。

## 公開に必要な作業

このフォルダーの中身を、公開先サーバーのドキュメントルート、または任意の公開ディレクトリへアップロードしてください。

必ず `index.html` が公開URLの入口になる位置に配置してください。

例:

- `https://example.com/` で公開する場合  
  `index.html` をドキュメントルート直下に配置

- `https://example.com/meo-manual/` で公開する場合  
  `meo-manual` フォルダー内に `index.html` と `assets`、`manuals` を配置

## フォルダー構成

- `index.html`  
  トップページです。

- `manuals/`  
  各説明書ページです。

- `assets/site.css`  
  サイト全体のデザインです。

- `assets/search.js`、`assets/search-index.js`  
  サイト内検索用のファイルです。

- `assets/manual-images/`  
  Word説明書から抽出した本文中の画像です。

- `assets/previews/`  
  トップページのカード用プレビュー画像です。

- `assets/pdfs/`  
  PDFで見るための説明書PDFです。

- `assets/brand/`  
  ロゴ画像です。

## 動作確認

公開後、以下を確認してください。

1. トップページが開く
2. 説明書カードをクリックして詳細ページへ移動できる
3. 検索欄で「クチコミ」「投稿」「順位」「GBP」「写真」などを検索できる
4. 検索結果をクリックすると該当説明書の該当箇所へ移動できる
5. 各説明書ページの「PDFで見る」リンクが開く

## 注意点

- `http://127.0.0.1:4173/` は制作用のローカル確認URLです。外部公開URLではありません。
- このサイトは静的サイトなので、PHPやデータベース設定は不要です。
- PDFと画像を含むため、アップロード容量は大きめです。
- サーバーにアップロードする際は、フォルダー構成を崩さないでください。
- 閲覧前にパスワードをかける場合は、`WEB担当者向け_パスワード設定手順.md` を確認してください。

## 更新方法

説明書のWordファイルを更新した場合は、制作者側でサイトを再生成してから、同じフォルダー一式を再アップロードしてください。

差し替え時は、古い `index.html`、`manuals`、`assets` を新しいものに置き換えれば更新できます。
